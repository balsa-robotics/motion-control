#pragma once

// The mode state machine is a deferred architectural decision.
// See `openspec/project.md` ("Deferred architectural decisions") for context;
// the design will be settled in a future change proposal that proposes the
// mode set, transitions, and arbitration rules. Until then, this directory
// reserves the name and the namespace.

namespace motion_control::mode {
// Layout placeholder. Real declarations land when the mode design is settled.
} // namespace motion_control::mode
