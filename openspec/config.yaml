schema: spec-driven

context: "@openspec/project.md"

# Per-artifact rules (optional)
rules:
  proposal:
    - Always include a "Non-goals" section
    - Note if the change touches the RT path (real-time constraints apply)
  tasks:
    - Break tasks into chunks of max 2 hours
    - Tag tasks that touch the RT path with [RT]
